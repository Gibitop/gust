fn status(enabled: bool): string {
    if enabled {
        return "Enabled"
    } else if false {
        return "Unreachable"
    } else {
        return "Disabled"
    }
}

fn main() {
    io.println(status(true))
}
