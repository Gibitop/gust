fn greet(name: String): String {
    return "Hello, " + name
}

fn answer(): u64 {
    return 42
}

fn main() {
    let message = greet("Gust")
    let value = answer()

    io.println(message + "!")
}
