struct Box<T> {
    value: T

    static fn new(value: T) => Self {
        value: value,
    }

    fn get() {
        return self.value
    }

    fn replace(mut self, value: T) {
        self.value = value
    }
}

struct Pair<T> {
    first: T
    second: T
}

fn main() {
    let mut number = Box {
        value: 42,
    }
    let constructed = Box<u8>.new(7)
    let message: Box<String> = Box {
        value: "Generics work!",
    }
    let nested = Box.new(
        Pair {
            first: 1,
            second: 2,
        },
    )

    number.replace(43)
    io.println(number.get().toString())
    io.println(constructed.get().toString())
    io.println(message.get())
    io.println(nested.get().second.toString())
}
