from ./greeting import greet
from ./greeting import { Greeter as GreeterType }
from ../fib import { fib }

fn main() {
    let greeter = GreeterType { name: "Gust" }
    io.println(greet.greeting(greeter))

    let result = fib(10)
    io.println(result.toString())
}
