from ../fib import { fib }
export struct Greeter {
    name: string
}

fn punctuation(): string => "!"

export fn greeting(greeter: Greeter): string {
    return "Hello, " + greeter.name + punctuation() + fib(10).toString()
}
