from ../fib import { fib }
struct Greeter {
    name: String
}

fn punctuation(): String => "!"

fn greeting(greeter: Greeter): String {
    return "Hello, " + greeter.name + punctuation() + fib(10).toString()
}
